pointCreate(x, y):
    var yMult = y * 100
    var point = x + yMult
    return point

getX(point):
    var px = point % 100
    return px

getY(point):
    var px = point % 100
    var tempY = point - px
    var py = tempY / 100
    return py