#copycat "util/arrays.miau"

main:
    call arrayCreate()

    var a = 1

    loop:
        call arrayAdd(result, "Sus")
        meow (a)
        call arrayPrint(result)
        meow (a)
        var a = a + 1
        goto loop

